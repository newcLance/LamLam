# 2026-05-21

## CF：虹 配色方案 v1（基于真实 UI 素材提炼）
- 输出：`d:\ecnalClaw\output\game-tracker-v2\dist\cfh-color-system.html`
- 提炼方法：用 Pillow `quantize(colors=12, MAXCOVERAGE)` 对 cfh 官网先导预约页三屏（home-1/2/3）做像素级量化采样，统计像素占比。
- 修正了之前在 `cfh-pitch.html` 里凭直觉给出的"暗蓝绿+猩红"假设——实际素材是**暖中性暗调（沥青褐/烟黑/石青金属）+ 干涸血红 + 偶发篝火橙**。
- 关键发现：
  1. 纯黑占据 **75–97%** 屏幕——"黑场为主，主体浮岛"是核心视觉策略
  2. Logo 红是 **#721D20 干涸血红**（不是亮红 #FF3B3B）
  3. **#FF6A2C 篝火橙**是被忽略的暖光锚点（home-3 左侧火光）
  4. **#B4C9C1 月光绿灰**在三屏里都出现，是 Logo 红的"安全色对位"
- 核心色板（13 个 token）：
  - Tier1 黑场：void / asphalt / steel / tar / gunmetal
  - Tier2 危险：blood-deep / blood / alert
  - Tier3 辅色：moss / moonlit / flesh / ember
  - 文字白用 #E6D9D8（带粉灰）替代纯白
- 比例规则：**70/22/7/1**（黑场 70% / 中性暗调 22% / 自然辅色 7% / 危险红 ≤1%）
- 含 6 条红线（不要冷蓝紫 / 不要高饱和绿黄 / 不要红橙叠 / 不要纯白 / 不要冷蓝绿主调 / 不要金黄）
- 含 5 阶 SAN 值色彩阶梯（清明→紧张→焦虑→幻觉→崩溃）

## 经验沉淀
- 做"配色提炼"时，**永远从真实素材采样**而不是凭描述脑补——这次官方网页用的是暖中性而非冷蓝绿，差距大到必须修正。
- 提色快捷流程：`PIL.Image.quantize(colors=N, method=MAXCOVERAGE)` → 转 RGB → `getdata()` → Counter——10 行能跑出可用的 dominant palette。
- 此前 `cfh-pitch.html` 第 3.2 章节配色基于错误假设，下次更新可考虑用此版色板覆盖（或保留两版做对比）。

## 11:00 追加 · 宣讲材料覆盖完成
- 之前的 `dist/cfh-pitch.html` 在 build.py 重新构建时被清理掉了（dist 是构建产物目录）。
- 重建后放在 `static/cfh-pitch.html`（源），同步部署到 `dist/cfh-pitch.html`，9 page 全员到位。
- 第 3.2 章节用 v1 真实采样色板**全量替换**了原"暗蓝绿+猩红"假设：
  - 全局 CSS variables 切换：`--bg/-2/-3` → `--void/--asphalt/--tar`，`--accent` 由 `#FF3B3B` 改为 `#C93030`，引入 `--moonlit/--ember/--flesh/--blood/--blood-deep` 系列
  - 章节内嵌 6 个区块：A 提炼来源（含三张官方截图）/ B 核心色板（13 token）/ C 70-22-7-1 比例条 / D DO·DON'T 规则 / E SAN 5 阶梯 / F Token 表
  - 移除原 "克系绿 #1F3A3A / 理智青 #5AD9C4" 等冷蓝绿色（只保留低饱和月光绿灰 #B4C9C1）
- 整页的视觉调性也跟着切——hero 渐变从蓝绿改为干涸红+苔藓褐，雷达图描边从 #FF3B3B 改为 #C93030，HUD HP 从亮红改 alert 红
- 工程提示：Write 工具单文件 ≥ 1000 行容易被截断，**首次写源 + Python 脚本追加**已成稳定二段式工作流
- 侧边栏 3.2 标记 ★ 提示用户该节为重点更新

## CF虹 项目级 SOP（写入 MEMORY 候选）
- **配色提炼必须从真实素材采样**：用 PIL `quantize` 跑像素占比，不要凭官方文案/视频描述脑补
- **dist/ 是构建产物**：直接写 dist 文件的话，下次 build.py 会清掉；要永久保留必须放 static/
- **图片相对路径在 static/ 与 dist/ 不同**：static 下要走 `../screenshots/`，dist 下要走 `screenshots/`，复制部署时务必同步替换

## 13:50 追加 · 项目组放出 17 张静态帧 → 订正报告 v2
- 用户从桌面 `CF虹档案/静态帧/` 给到 17 张新 UI 截图：调查员档案 1-3 / 匹配画面 1-7 / 基建 1-4 / 线索搜集 1-2 / 大脑受冲击。
- 已统一拷贝到 `dist/screenshots/crossfire-hong/static-frames/` 备用。
- 输出订正报告：`d:\ecnalClaw\output\game-tracker-v2\dist\cfh-corrections-v2.html`
- **11 项订正**（4 P0 + 5 P1 + 2 P2）：

### P0 重大订正（必改）
1. **地图名错**：`夜幕山庄` → `灰湾镇`（雪原小镇，不是哥特别墅整图）；具体 POI 含「灰湾镇美术馆」；哥特只是 POI 室内细节。
2. **核心循环不全**：v1 只写局内塔可夫式，**遗漏局外"基石"养成**——4 部门（配给部/研发部/勘探部/预演部）+ 基石总等级。正确循环 = "撤离循环 + Roguelite 长期养成"。
3. **角色系统被严重低估**：实际是 **10+ 具体角色 + 六维属性**（Strength/Physique/Agile/Esthesia/Resolve/Lucky）= COC 跑团车卡。已确认角色：SWAT / KUI(葵·Team Aoi) / SPIRIT FOX / WHITE WOLF / PHANTOM / FOX HUNTER / SWORDS / MUTANT / LENTEMENT。
4. **虹蚀色谱完全错**：实际是**三档浓度 + 三色光谱**（低=翡翠绿 / 中=金黄褐 / 高=烈焰红），且**SAN 视觉本征是青绿+淡紫蓝彩虹油膜（克苏鲁本征"不可名状之色"），不是猩红泛屏**。

### P1 重要补充
- **污染值机制**（武器/装备反向污染玩家）—— v1 完全没提
- **线索搜集 = 摄像机调查**（Outlast 风手持 DV+REC 计时+取景框+F收集）
- **武器系统更"标准 CF"**：6 大类 + 4 档稀有度（绿蓝紫金）+ 弹药细分 + 耐久度 + 可回收
- **怪物官方代号 = 「幽影」**（Phantom，B/A/S 级 + 强化变体），v1 一直叫"克系怪物"是描述
- **大厅美学 = X档案/寂静岭式 1980s 调查工作室**（CRT+老电话+案件白板），不是哥特废墟。这是被忽略的"第二条美学主线"
- **货币三轨 + 排位**：金币 9.9M / 黄色小金条 1001 / 红色虹蚀晶石 100,101；"开启排位"=有竞技排位

### P0 配色重新分场景（重采样）
- **场景 A 基地**：冷蓝青 #32535E (57%) — CRT 工作室
- **场景 B POI**：暗暖中性 #470 + 黑场 79% — 之前 v1 的暖中性结论只在这一类成立
- **场景 C 虹蚀状态**：青绿 #7AD7D8 + 淡紫蓝 #9FA2DD — 彩虹油膜，无红色
- 修正后的色彩语义：红 ≠ 危险通用色，红只是"高浓度暴露"那一档
- **窗口标题确认项目代号 = "CFR"**

### 经验沉淀
- 用户可能持续给新素材，订正循环要做成"<u>报告先 → 用户拍板 → 直接改源</u>"二段式，避免每次主动覆盖宣讲材料
- 桌面新素材的处理标准动作：① 拷贝到 dist/screenshots/.../static-frames ② 像素采样验证色板假设 ③ 出对照报告 ④ 等用户拍板再覆盖正本
- 这次发现：v1 把"夜幕山庄/克苏鲁哥特别墅"当成核心地图——是从 v0 网络资料抄来的，可能源于早期 PV 误读。说明<u>没有第一手 UI 素材时，所有"地点/系统/角色"描述都应标注"待确认"</u>
